{
  "title": "NIH Grants MCP",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool Name",
      "type": "string",
      "description": "MCP tool to call",
      "enum": [
        "search_grants",
        "get_grant_details",
        "find_grant_citations",
        "organization_funding_profile",
        "researcher_profile",
        "funding_trends"
      ],
      "editor": "select",
      "prefill": "search_grants"
    },
    "params": {
      "title": "Parameters (JSON)",
      "type": "object",
      "description": "Tool parameters as JSON object",
      "editor": "json",
      "prefill": {"query": "machine learning", "limit": 10}
    }
  }
}
